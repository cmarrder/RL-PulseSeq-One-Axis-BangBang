To profile the timings of function calls, add the following three lines in CMakeLists.txt :

SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -pg")
SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -pg")
SET(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -pg")

Then, in the build subdirectory, make sure there is a gmon.out file.
Then, in the build subdirectory, run the following command to write profile data to output.txt :
gprof -l learn gmon.out > output.txt
